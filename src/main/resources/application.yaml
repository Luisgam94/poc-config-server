server:
  port: '8888'
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Luisgam94/config-properties
          skipSslValidation: true
          timeout: 4
          #uri: file:///C:/Users/LUISG/Desktop/pocs/config-repo
  # Spring properties in case you use different profiles
